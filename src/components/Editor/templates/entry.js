export const entry = (title) => {
  return (
    `
      #Define ${title} briefly in a sentence

      #Give some information about ${title}

      #Give your opinions related to that information
   `
  )
}
